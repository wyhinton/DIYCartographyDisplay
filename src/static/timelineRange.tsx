import { TimeRange } from "pondjs";

const startDate = new Date(1763, 0, 1);
const endDate = new Date(2020, 0, 1);
const timelineRange = new TimeRange(startDate, endDate);

export default timelineRange;
